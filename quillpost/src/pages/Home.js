import Base from "../components/Base";

const Home = () => {
    return (
        <Base>
            <h1>This is Home page..</h1>
            <p>Wlcome to Home page</p>
        </Base>
    );
};

export default Home;